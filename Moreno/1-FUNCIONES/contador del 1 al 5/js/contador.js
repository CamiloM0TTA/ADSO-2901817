
function conta(pnumero){ // con parametros
    let numero=pnumero;
    let contador=0;
    while(contador<numero){
        contador++
        console.log(contador);
    }
    return contador;
}
const contaExpresion=function(pnumero){ // con expresion
    let numero=pnumero;
    let contador=0;
    while(contador<numero){
        contador++
        console.log(contador);
    }
    return contador;
}

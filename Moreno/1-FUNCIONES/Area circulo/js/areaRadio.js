let area;
const pi=3.1416;

function radio(pradio){ // con parametros
    let area;
    const pi=3.1416;
    let radio=pradio;
    area= pi *(radio*radio)
    return area;
}
const radioExpresion=function(pradio){ // con expresion
    let radio=pradio;
    area= pi *(radio*radio)
    return area;
}